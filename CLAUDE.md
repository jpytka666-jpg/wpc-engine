# Claude handoff — Ghost Gate

This branch owns the isolated network VM boundary for AIONS.
Use explicit egress policy and keep networking outside AIONS by default.
Do not modify unrelated modules or existing local AIONS workspaces.
GitHub first; local VM deployment only after security checks and CI acceptance.
