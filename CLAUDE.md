# Claude handoff — Memory / System Graph

This branch owns the interactive system and memory graph.
Graph entities must correspond to real AIONS IDs, code symbols, processes, memories, or dependencies.
Do not modify unrelated modules or existing local AIONS workspaces.
GitHub first; local implementation only after interfaces and CI acceptance.
