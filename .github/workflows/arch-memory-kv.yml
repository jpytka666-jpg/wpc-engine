name: AIONS Architecture - Memory KV
on:
  push:
    branches: ["arch/memory-kv"]
  pull_request:
    branches: [main]
    paths: ["CLAUDE.md", "docs/**", ".github/workflows/arch-memory-kv.yml"]
permissions:
  contents: read
jobs:
  contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify contract
        run: |
          test -f CLAUDE.md
          test -f docs/MODULE-CONTRACT.md
          test -f docs/IMPLEMENTATION-STAGE-1.md
          grep -q "Memory / KV" docs/MODULE-CONTRACT.md
          grep -q "GitHub" docs/IMPLEMENTATION-STAGE-1.md
