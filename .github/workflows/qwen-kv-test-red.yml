# Retired 2026-08-24 by ChatGPT.
# Reason: this workflow attempted blind source rewriting and caused false RED
# failures when the Qwen cache constructor evolved. The KV probe is now wired
# directly in Rust and verified by the normal CI gate. Kept as documentation;
# it must never modify repository files automatically.
name: Qwen KV Probe Patcher (retired)

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired on 2026-08-24 — Rust KV probe is now source-controlled; no automatic patching is permitted."
