# Retired 2026-08-24 by ChatGPT.
# Reason: this workflow attempted blind Gemma source rewriting and was not the
# correct mechanism for the Qwen resident-KV probe. Source wiring is now
# explicit in Rust and verified by normal CI. Kept as historical documentation;
# it never modifies repository files automatically.
name: Gemma KV Probe Patcher (retired)

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired on 2026-08-24 — no automatic source patching is permitted."
