name: AIONS Architecture - Studio
on:
  push:
    branches: ["arch/studio"]
  pull_request:
    branches: [main]
    paths: ["CLAUDE.md", "docs/**", ".github/workflows/arch-studio.yml"]
permissions:
  contents: read
jobs:
  contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify contract
        run: |
          test -f CLAUDE.md
          test -f docs/MODULE-CONTRACT.md
          test -f docs/IMPLEMENTATION-STAGE-1.md
          grep -q "AIONS Studio" docs/MODULE-CONTRACT.md
          grep -q "GitHub" docs/IMPLEMENTATION-STAGE-1.md
