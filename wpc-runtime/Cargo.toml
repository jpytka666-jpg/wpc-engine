[package]
name = "wpc-runtime"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0"
matrixmultiply = "0.3"
libc = "0.2"
rand = "0.8"

[dev-dependencies]
criterion = { version = "0.4", features = ["html_reports"] }
